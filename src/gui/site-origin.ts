/**
 * The project's own public website — the one origin the open-source client may
 * know about. Services a managed deployment offers (an identity service, the
 * desktop update feed) are DISCOVERED from manifests served here, so no other
 * hosted infrastructure is ever named in this repo.
 */
export const PUBLIC_SITE_ORIGIN = 'https://latticesql.com';
