// Auto-composed section of the GUI stylesheet. Verbatim substring of the original
// css template literal — do not hand-edit; see styles/index.ts for composition.
export const sidebarCss = `    /* ── Slim / collapsible left sidebar ────────────────── */
    /* Advanced-mode toggle at the top of the sidebar. */
    .sidebar-advanced {
      margin: 0 0 12px 2px; padding: 4px 6px; border-radius: 6px;
    }
    .sidebar-advanced:hover { background: var(--row-hover); }
    .sidebar-advanced .toggle-label { font-size: 12px; color: var(--text-muted); }

`;
