// Auto-composed section of the GUI stylesheet. Verbatim substring of the original
// css template literal — do not hand-edit; see styles/index.ts for composition.
export const settingsGearCss = `    /* ── Header settings gear (top-right) ───────────────── */
    #settings-gear {
      margin-left: auto; display: inline-flex; align-items: center; justify-content: center;
      width: 32px; height: 32px; background: transparent; border: 1px solid #e6eaf0;
      border-radius: 6px; cursor: pointer; color: #0f172a; flex-shrink: 0;
    }
    #settings-gear:hover { background: rgba(15, 23, 42, 0.04); }
    #settings-gear svg { width: 18px; height: 18px; display: block; }

`;
