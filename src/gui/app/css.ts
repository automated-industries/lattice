// Extracted verbatim from app.ts — the GUI stylesheet (static template, no interpolation).
// Composed from per-section segments under ./styles/; see ./styles/index.ts.
export { css } from './styles/index.js';
