// Extracted verbatim from app.ts — the GUI client script (static template, no interpolation).
// Composed from per-subsystem segments under ./modules/; see ./modules/index.ts.
export { appJs } from './modules/index.js';
